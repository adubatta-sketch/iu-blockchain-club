<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blockchain Club at IU</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Times New Roman', Times, serif;
            background: #0a0a0a;
            color: #ffffff;
            overflow-x: hidden;
        }

        /* Animated Network Background */
        #network-canvas {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
        }

        /* Floating Crypto Symbols */
        .floating-symbols {
            position: fixed;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1;
            font-family: Arial, sans-serif;
        }

        .symbol {
            position: absolute;
            opacity: 0.15;
            font-weight: bold;
            animation: float 25s infinite ease-in-out;
            color: #990000;
        }

        .symbol:nth-child(1) { top: 5%; left: 5%; font-size: 40px; animation-delay: 0s; }
        .symbol:nth-child(2) { top: 15%; right: 8%; font-size: 35px; animation-delay: 2s; }
        .symbol:nth-child(3) { top: 30%; left: 12%; font-size: 45px; animation-delay: 4s; }
        .symbol:nth-child(4) { top: 50%; right: 10%; font-size: 38px; animation-delay: 6s; }
        .symbol:nth-child(5) { bottom: 20%; left: 8%; font-size: 42px; animation-delay: 8s; }
        .symbol:nth-child(6) { bottom: 35%; right: 15%; font-size: 36px; animation-delay: 10s; }
        .symbol:nth-child(7) { top: 70%; left: 20%; font-size: 40px; animation-delay: 12s; }
        .symbol:nth-child(8) { top: 40%; right: 25%; font-size: 44px; animation-delay: 14s; }

        @keyframes float {
            0%, 100% {
                transform: translate(0, 0) rotate(0deg);
            }
            25% {
                transform: translate(40px, -40px) rotate(90deg);
            }
            50% {
                transform: translate(-30px, 30px) rotate(180deg);
            }
            75% {
                transform: translate(35px, 25px) rotate(270deg);
            }
        }

        header {
            position: fixed;
            top: 0;
            width: 100%;
            background: rgba(10, 10, 10, 0.9);
            backdrop-filter: blur(15px);
            padding: 1.5rem 0;
            z-index: 1000;
            border-bottom: 1px solid rgba(153, 0, 0, 0.3);
        }

        nav {
            max-width: 1400px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 3rem;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: bold;
            color: #990000;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .nav-links {
            display: flex;
            gap: 2.5rem;
            list-style: none;
            align-items: center;
        }

        .nav-links a {
            color: #cccccc;
            text-decoration: none;
            transition: color 0.3s;
            font-weight: 500;
        }

        .nav-links a:hover {
            color: #990000;
        }

        .hero {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 8rem 2rem 4rem;
            position: relative;
            z-index: 2;
        }

        .hero h1 {
            font-size: 5rem;
            font-weight: 900;
            margin-bottom: 1rem;
            line-height: 1.1;
        }

        .hero h1 .red {
            color: #990000;
        }

        .hero h1 .white {
            color: #ffffff;
        }

        .hero .subtitle {
            font-size: 1.8rem;
            color: #cccccc;
            margin-bottom: 1.5rem;
        }

        .hero .subtitle .highlight {
            color: #990000;
        }

        .hero .description {
            font-size: 1.2rem;
            color: #999999;
            max-width: 800px;
            margin: 0 auto 3rem;
            line-height: 1.8;
        }

        .cta-buttons {
            display: flex;
            gap: 1.5rem;
            flex-wrap: wrap;
            justify-content: center;
        }

        .btn-primary {
            background: #990000;
            color: white;
            padding: 1.2rem 3rem;
            border: none;
            border-radius: 50px;
            font-size: 1.1rem;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }

        .btn-primary:hover {
            background: #cc0000;
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(153, 0, 0, 0.4);
        }

        .btn-secondary {
            background: transparent;
            color: #990000;
            padding: 1.2rem 3rem;
            border: 2px solid #990000;
            border-radius: 50px;
            font-size: 1.1rem;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s;
            text-decoration: none;
        }

        .btn-secondary:hover {
            background: #990000;
            color: white;
            transform: translateY(-3px);
        }

        section {
            max-width: 1400px;
            margin: 0 auto;
            padding: 6rem 3rem;
            position: relative;
            z-index: 2;
        }

        h2 {
            font-size: 3rem;
            margin-bottom: 3rem;
            text-align: center;
            color: #ffffff;
        }

        h2 .red {
            color: #990000;
        }

        .crypto-ticker {
            background: rgba(20, 20, 20, 0.6);
            border: 1px solid rgba(153, 0, 0, 0.3);
            padding: 3rem;
            margin: 4rem auto;
            border-radius: 20px;
            backdrop-filter: blur(10px);
        }

        .crypto-ticker h3 {
            color: #990000;
            margin-bottom: 2rem;
            text-align: center;
            font-size: 2rem;
        }

        .crypto-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 1.5rem;
        }

        .crypto-item {
            background: rgba(30, 30, 30, 0.8);
            padding: 1.5rem;
            border-radius: 15px;
            text-align: center;
            transition: all 0.3s;
            cursor: pointer;
            border: 1px solid rgba(153, 0, 0, 0.2);
        }

        .crypto-item:hover {
            transform: translateY(-8px);
            border-color: #990000;
            box-shadow: 0 10px 30px rgba(153, 0, 0, 0.3);
        }

        .crypto-name {
            font-weight: bold;
            color: #ffffff;
            margin-bottom: 0.5rem;
            font-size: 1.1rem;
        }

        .crypto-price {
            font-size: 1.8rem;
            color: #990000;
            margin-bottom: 0.3rem;
            font-weight: bold;
        }

        .crypto-change {
            font-size: 0.9rem;
        }

        .positive {
            color: #10b981;
        }

        .negative {
            color: #ef4444;
        }

        .about-content {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 2rem;
            margin-top: 3rem;
        }

        .card {
            background: rgba(20, 20, 20, 0.6);
            padding: 2.5rem;
            border-radius: 20px;
            border: 1px solid rgba(153, 0, 0, 0.3);
            transition: all 0.3s;
            position: relative;
            overflow: hidden;
            backdrop-filter: blur(10px);
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background: linear-gradient(90deg, #990000, #cc0000);
        }

        .card:hover {
            transform: translateY(-10px);
            border-color: #990000;
            box-shadow: 0 15px 40px rgba(153, 0, 0, 0.3);
        }

        .card h3 {
            color: #990000;
            margin-bottom: 1rem;
            font-size: 1.6rem;
        }

        .card p {
            color: #cccccc;
            line-height: 1.8;
        }

        .calendar-container {
            background: rgba(20, 20, 20, 0.6);
            padding: 3rem;
            border-radius: 20px;
            border: 1px solid rgba(153, 0, 0, 0.3);
            margin-top: 3rem;
            backdrop-filter: blur(10px);
        }

        .event-item {
            background: rgba(30, 30, 30, 0.8);
            padding: 2rem;
            border-radius: 15px;
            border-left: 4px solid #990000;
            margin-bottom: 2rem;
            transition: all 0.3s;
            cursor: pointer;
        }

        .event-item:hover {
            transform: translateX(15px);
            box-shadow: 0 10px 30px rgba(153, 0, 0, 0.3);
            border-left-width: 8px;
        }

        .event-date {
            color: #990000;
            font-weight: bold;
            margin-bottom: 0.8rem;
            font-size: 1.2rem;
        }

        .event-title {
            font-size: 1.5rem;
            margin-bottom: 0.8rem;
            color: #ffffff;
        }

        .event-item p {
            color: #cccccc;
            line-height: 1.6;
        }

        .event-link {
            color: #990000;
            text-decoration: none;
            font-size: 1rem;
            transition: opacity 0.3s;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            margin-top: 0.5rem;
        }

        .event-link:hover {
            opacity: 0.7;
        }

        .resources-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
            margin-top: 3rem;
        }

        .resource-card {
            background: rgba(20, 20, 20, 0.6);
            padding: 2.5rem;
            border-radius: 20px;
            border: 1px solid rgba(153, 0, 0, 0.3);
            transition: all 0.3s;
            text-align: center;
            backdrop-filter: blur(10px);
        }

        .resource-card:hover {
            transform: scale(1.05) rotate(2deg);
            border-color: #990000;
            box-shadow: 0 15px 40px rgba(153, 0, 0, 0.4);
        }

        .resource-icon {
            font-size: 3.5rem;
            margin-bottom: 1.5rem;
        }

        .resource-card h3 {
            color: #990000;
            margin-bottom: 1rem;
            font-size: 1.5rem;
        }

        .resource-card p {
            color: #cccccc;
            margin-bottom: 1.5rem;
            line-height: 1.6;
        }

        .resource-button {
            background: #990000;
            color: white;
            padding: 0.9rem 2rem;
            border-radius: 25px;
            text-decoration: none;
            display: inline-block;
            transition: all 0.3s;
            font-weight: bold;
        }

        .resource-button:hover {
            background: #cc0000;
            transform: translateY(-3px);
        }

        .team-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2.5rem;
            margin-top: 3rem;
        }

        .team-member {
            text-align: center;
            background: rgba(20, 20, 20, 0.6);
            padding: 2rem;
            border-radius: 20px;
            border: 1px solid rgba(153, 0, 0, 0.3);
            transition: all 0.3s;
        }

        .team-member:hover {
            transform: translateY(-10px);
            border-color: #990000;
            box-shadow: 0 15px 40px rgba(153, 0, 0, 0.3);
        }

        .team-photo {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            background: linear-gradient(135deg, #990000 0%, #cc0000 100%);
            margin: 0 auto 1.5rem;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 2.5rem;
            font-weight: bold;
            transition: transform 0.5s;
            border: 3px solid rgba(153, 0, 0, 0.5);
        }

        .team-member:hover .team-photo {
            transform: rotate(360deg) scale(1.1);
        }

        .team-member h3 {
            color: #990000;
            margin-bottom: 0.5rem;
            font-size: 1.3rem;
        }

        .team-member .role {
            color: #cccccc;
            font-size: 1rem;
            font-weight: 500;
        }

        .time-clock-top {
            position: fixed;
            top: 20px;
            left: 20px;
            background: rgba(20, 20, 20, 0.8);
            padding: 1rem 1.5rem;
            border-radius: 15px;
            border: 1px solid rgba(153, 0, 0, 0.3);
            backdrop-filter: blur(10px);
            z-index: 1000;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        }
        
        .time-display {
            font-size: 1.2rem;
            font-weight: bold;
            color: #ffffff;
            font-family: 'Courier New', monospace;
            margin-bottom: 0.2rem;
        }
        
        .date-display {
            font-size: 0.9rem;
            color: #cccccc;
            font-weight: 500;
        }
        
        .timezone {
            font-size: 0.8rem;
            color: #990000;
            font-weight: bold;
            margin-top: 0.2rem;
        }
        
        .contact-spacing {
            margin-bottom: 2rem;
        }
        
        .night-mode-toggle {
            position: fixed;
            top: 50%;
            right: 20px;
            transform: translateY(-50%);
            background: #990000;
            color: white;
            border: none;
            border-radius: 50px;
            padding: 1rem;
            cursor: pointer;
            font-size: 1.5rem;
            z-index: 1000;
            transition: all 0.3s;
            box-shadow: 0 5px 15px rgba(153, 0, 0, 0.3);
        }
        
        .night-mode-toggle:hover {
            background: #cc0000;
            transform: translateY(-50%) scale(1.1);
        }
        
        .night-mode-toggle.active {
            background: #ffd700;
            color: #000;
        }
        
        /* Light mode specific styles */
        body.light-mode {
            background: #f8f9fa !important;
            color: #212529 !important;
        }
        
        body.light-mode header {
            background: rgba(248, 249, 250, 0.9) !important;
            border-bottom: 1px solid rgba(153, 0, 0, 0.2) !important;
        }
        
        body.light-mode .nav-links a {
            color: #495057 !important;
        }
        
        body.light-mode .nav-links a:hover {
            color: #990000 !important;
        }
        
        body.light-mode .time-clock-top {
            background: rgba(255, 255, 255, 0.9) !important;
            border: 1px solid rgba(153, 0, 0, 0.3) !important;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1) !important;
        }
        
        body.light-mode .time-display {
            color: #212529 !important;
        }
        
        body.light-mode .date-display {
            color: #6c757d !important;
        }
        
        body.light-mode .card, 
        body.light-mode .crypto-item, 
        body.light-mode .event-item, 
        body.light-mode .resource-card, 
        body.light-mode .team-member, 
        body.light-mode .crypto-ticker {
            background: rgba(255, 255, 255, 0.9) !important;
            border: 1px solid rgba(153, 0, 0, 0.2) !important;
            color: #212529 !important;
        }
        
        body.light-mode .card p, 
        body.light-mode .crypto-item p, 
        body.light-mode .event-item p, 
        body.light-mode .resource-card p, 
        body.light-mode .team-member p {
            color: #495057 !important;
        }
        
        body.light-mode .crypto-name {
            color: #212529 !important;
        }
        
        body.light-mode .event-title {
            color: #212529 !important;
        }
        
        body.light-mode .contact-info p {
            color: #495057 !important;
        }
        
        body.light-mode footer {
            background: rgba(248, 249, 250, 0.9) !important;
            border-top: 1px solid rgba(153, 0, 0, 0.2) !important;
            color: #495057 !important;
        }
        
        body.light-mode .social-links a {
            color: #495057 !important;
        }
        
        body.light-mode .social-links a:hover {
            color: #990000 !important;
        }
        
        body.light-mode input, 
        body.light-mode textarea {
            background: rgba(255, 255, 255, 0.9) !important;
            color: #212529 !important;
            border: 2px solid rgba(153, 0, 0, 0.3) !important;
        }
        
        body.light-mode input:focus, 
        body.light-mode textarea:focus {
            border-color: #990000 !important;
            box-shadow: 0 0 20px rgba(153, 0, 0, 0.2) !important;
        }

        .contact-info {
            text-align: center;
            margin-bottom: 3rem;
        }

        .contact-info p {
            color: #cccccc;
            font-size: 1.2rem;
            margin-bottom: 1rem;
        }

        .contact-info a {
            color: #990000;
            text-decoration: none;
            font-weight: bold;
            font-size: 1.3rem;
        }

        .contact-info a:hover {
            opacity: 0.8;
        }

        .form-group {
            margin-bottom: 2rem;
        }

        label {
            display: block;
            margin-bottom: 0.8rem;
            color: #990000;
            font-weight: bold;
            font-size: 1.1rem;
        }

        input, textarea {
            width: 100%;
            padding: 1rem;
            border: 2px solid rgba(153, 0, 0, 0.3);
            border-radius: 10px;
            font-size: 1rem;
            transition: all 0.3s;
            background: rgba(20, 20, 20, 0.6);
            color: #ffffff;
        }

        input:focus, textarea:focus {
            outline: none;
            border-color: #990000;
            box-shadow: 0 0 20px rgba(153, 0, 0, 0.3);
        }

        textarea {
            min-height: 150px;
            resize: vertical;
        }

        footer {
            background: rgba(10, 10, 10, 0.9);
            color: #cccccc;
            text-align: center;
            padding: 3rem 2rem;
            margin-top: 6rem;
            position: relative;
            z-index: 2;
            border-top: 1px solid rgba(153, 0, 0, 0.3);
        }

        .social-links {
            display: flex;
            justify-content: center;
            gap: 2rem;
            margin-top: 1.5rem;
        }

        .social-links a {
            color: #cccccc;
            text-decoration: none;
            transition: all 0.3s;
            font-weight: 500;
        }

        .social-links a:hover {
            color: #990000;
            transform: translateY(-3px);
        }

        @media (max-width: 768px) {
            .hero h1 {
                font-size: 3rem;
            }

            .hero .subtitle {
                font-size: 1.3rem;
            }

            .nav-links {
                gap: 1rem;
                font-size: 0.9rem;
            }

            h2 {
                font-size: 2rem;
            }

            .crypto-grid {
                grid-template-columns: 1fr;
            }

            .cta-buttons {
                flex-direction: column;
            }

            nav {
                padding: 0 1.5rem;
            }

            section {
                padding: 4rem 1.5rem;
            }
        }

        .loading {
            text-align: center;
            color: #999999;
            font-style: italic;
        }

        #rotatingTopic {
            transition: opacity 0.3s ease-in-out;
        }
    </style>
</head>
<body>
    <canvas id="network-canvas"></canvas>

    <div class="floating-symbols">
        <div class="symbol">₿</div>
        <div class="symbol">Ξ</div>
        <div class="symbol">⬡</div>
        <div class="symbol">◊</div>
        <div class="symbol">⟠</div>
        <div class="symbol">₿</div>
        <div class="symbol">Ξ</div>
        <div class="symbol">⬡</div>
    </div>

    <!-- Time Clock - Top Left -->
    <div class="time-clock-top" id="timeClockTop">
        <div class="time-display" id="current-time"></div>
        <div class="date-display" id="current-date"></div>
        <div class="timezone">EST</div>
    </div>
    
    <header>
        <nav>
            <div class="logo">⛓️ Blockchain at IU Club</div>
            <ul class="nav-links">
                <li><a href="#about">About</a></li>
                <li><a href="#prices">Prices</a></li>
                <li><a href="#events">Events</a></li>
                <li><a href="#resources">Resources</a></li>
                <li><a href="#team">Team</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
        </nav>
    </header>
    
    <!-- Night Light Toggle Button -->
    <button class="night-mode-toggle" id="nightModeToggle" title="Toggle Night/Light Mode">🌙</button>

    <div class="hero">
        <h1>
            <span class="red">Blockchain Club</span><br>
            <span class="white">at Indiana</span><br>
            <span class="red">University</span>
        </h1>
        <div class="subtitle">Exploring <span class="highlight">Future Tech</span></div>
        <p class="description">Join Indiana University's premier blockchain club where we teach, explore, and apply cutting-edge blockchain technology and digital assets in real-world contexts.</p>
        <div class="cta-buttons">
            <a href="https://groupme.com/join_group/111029466/mh2tLBjm" target="_blank" class="btn-primary">Join GroupMe →</a>
            <a href="#events" class="btn-secondary">Meeting Schedule</a>
        </div>
    </div>

    <div id="prices" class="crypto-ticker">
        <h3>📊 Live Crypto Prices</h3>
        <div id="cryptoPrices" class="crypto-grid">
            <div class="loading">Loading prices...</div>
        </div>
    </div>

    <section style="max-width: 1400px; margin: 2rem auto; padding: 0 3rem; position: relative; z-index: 2;">
        <div style="background: rgba(20, 20, 20, 0.6); border: 1px solid rgba(153, 0, 0, 0.3); padding: 2.5rem; border-radius: 20px; backdrop-filter: blur(10px);">
            <h3 style="color: #990000; margin-bottom: 1.5rem; font-size: 2rem; text-align: center;">📰 Crypto Learning Resources</h3>
            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 1.5rem;">
                <a href="https://www.investopedia.com/terms/b/blockchain.asp" target="_blank" style="background: rgba(30, 30, 30, 0.8); padding: 1.5rem; border-radius: 12px; border: 1px solid rgba(153, 0, 0, 0.2); text-decoration: none; color: inherit; transition: all 0.3s; display: block;" onmouseover="this.style.borderColor='#990000'; this.style.transform='translateY(-5px)'" onmouseout="this.style.borderColor='rgba(153, 0, 0, 0.2)'; this.style.transform='translateY(0)'">
                    <div style="font-size: 2rem; margin-bottom: 0.5rem;">⛓️</div>
                    <h4 style="color: #990000; margin-bottom: 0.5rem;">What is Blockchain?</h4>
                    <p style="color: #cccccc; font-size: 0.95rem;">Distributed ledger technology explained</p>
                </a>
                <a href="https://ethereum.org/en/smart-contracts/" target="_blank" style="background: rgba(30, 30, 30, 0.8); padding: 1.5rem; border-radius: 12px; border: 1px solid rgba(153, 0, 0, 0.2); text-decoration: none; color: inherit; transition: all 0.3s; display: block;" onmouseover="this.style.borderColor='#990000'; this.style.transform='translateY(-5px)'" onmouseout="this.style.borderColor='rgba(153, 0, 0, 0.2)'; this.style.transform='translateY(0)'">
                    <div style="font-size: 2rem; margin-bottom: 0.5rem;">📝</div>
                    <h4 style="color: #990000; margin-bottom: 0.5rem;">Smart Contracts</h4>
                    <p style="color: #cccccc; font-size: 0.95rem;">Self-executing digital agreements</p>
                </a>
                <a href="https://www.coinbase.com/learn/crypto-basics/what-is-defi" target="_blank" style="background: rgba(30, 30, 30, 0.8); padding: 1.5rem; border-radius: 12px; border: 1px solid rgba(153, 0, 0, 0.2); text-decoration: none; color: inherit; transition: all 0.3s; display: block;" onmouseover="this.style.borderColor='#990000'; this.style.transform='translateY(-5px)'" onmouseout="this.style.borderColor='rgba(153, 0, 0, 0.2)'; this.style.transform='translateY(0)'">
                    <div style="font-size: 2rem; margin-bottom: 0.5rem;">💰</div>
                    <h4 style="color: #990000; margin-bottom: 0.5rem;">DeFi Explained</h4>
                    <p style="color: #cccccc; font-size: 0.95rem;">Decentralized financial services</p>
                </a>
                <a href="https://ethereum.org/en/dao/" target="_blank" style="background: rgba(30, 30, 30, 0.8); padding: 1.5rem; border-radius: 12px; border: 1px solid rgba(153, 0, 0, 0.2); text-decoration: none; color: inherit; transition: all 0.3s; display: block;" onmouseover="this.style.borderColor='#990000'; this.style.transform='translateY(-5px)'" onmouseout="this.style.borderColor='rgba(153, 0, 0, 0.2)'; this.style.transform='translateY(0)'">
                    <div style="font-size: 2rem; margin-bottom: 0.5rem;">🏛️</div>
                    <h4 style="color: #990000; margin-bottom: 0.5rem;">What are DAOs?</h4>
                    <p style="color: #cccccc; font-size: 0.95rem;">Decentralized autonomous organizations</p>
                </a>
                <a href="https://www.coinbase.com/learn/crypto-basics/what-is-web3" target="_blank" style="background: rgba(30, 30, 30, 0.8); padding: 1.5rem; border-radius: 12px; border: 1px solid rgba(153, 0, 0, 0.2); text-decoration: none; color: inherit; transition: all 0.3s; display: block;" onmouseover="this.style.borderColor='#990000'; this.style.transform='translateY(-5px)'" onmouseout="this.style.borderColor='rgba(153, 0, 0, 0.2)'; this.style.transform='translateY(0)'">
                    <div style="font-size: 2rem; margin-bottom: 0.5rem;">🌐</div>
                    <h4 style="color: #990000; margin-bottom: 0.5rem;">Web3 Explained</h4>
                    <p style="color: #cccccc; font-size: 0.95rem;">The decentralized internet</p>
                </a>
                <a href="https://metamask.io/" target="_blank" style="background: rgba(30, 30, 30, 0.8); padding: 1.5rem; border-radius: 12px; border: 1px solid rgba(153, 0, 0, 0.2); text-decoration: none; color: inherit; transition: all 0.3s; display: block;" onmouseover="this.style.borderColor='#990000'; this.style.transform='translateY(-5px)'" onmouseout="this.style.borderColor='rgba(153, 0, 0, 0.2)'; this.style.transform='translateY(0)'">
                    <div style="font-size: 2rem; margin-bottom: 0.5rem;">🦊</div>
                    <h4 style="color: #990000; margin-bottom: 0.5rem;">MetaMask Guide</h4>
                    <p style="color: #cccccc; font-size: 0.95rem;">Popular crypto wallet setup</p>
                </a>
            </div>
        </div>
    </section>

    <section id="about">
        <h2><span class="red">About</span> Us</h2>
        <div class="about-content">
            <div class="card">
                <h3>🎯 Our Mission</h3>
                <p>We aim to educate students about cryptocurrency, blockchain technology, and decentralized finance while fostering a community of innovators and enthusiasts at IU.</p>
            </div>
            <div class="card">
                <h3>📚 What We Do</h3>
                <p>Weekly workshops, guest speakers from the industry, hands-on trading simulations, blockchain development projects, and networking opportunities with crypto professionals.</p>
            </div>
            <div class="card">
                <h3>🤝 Who Can Join</h3>
                <p>All IU students are welcome! No prior experience needed. Whether you're a beginner or an experienced trader, there's a place for you in our community.</p>
            </div>
        </div>
    </section>

    <section id="events">
        <h2><span class="red">Events</span> Calendar</h2>
        <div class="calendar-container">
            <div class="event-item">
                <div class="event-date">📍 TBD</div>
                <div class="event-title">First General Meeting</div>
                <p>Join us for our inaugural meeting! We'll introduce the club, discuss our goals for the semester, and get to know fellow blockchain enthusiasts. Time and location to be announced soon.</p>
            </div>
            <div class="event-item">
                <div class="event-date">📍 December 5, 2025</div>
                <div class="event-title">Midwest Blockchain Conference 2025</div>
                <p>Join us for the premier blockchain event in the Midwest! Network with industry leaders, attend workshops, and learn about the latest developments in blockchain technology.</p>
                <a href="https://events.coinpedia.org/midwest-blockchain-conference-2025-7173/" target="_blank" class="event-link">🔗 Learn More & Register →</a>
            </div>
            <div class="event-item">
                <div class="event-date">📍 Every Wednesday, 6:00 PM</div>
                <div class="event-title">Weekly General Meetings</div>
                <p>Join us for discussions on the latest crypto trends, market analysis, and educational presentations. Location: TBA</p>
            </div>
            <div class="event-item">
                <div class="event-date">📍 Monthly Workshop Series</div>
                <div class="event-title">Blockchain Development Workshop</div>
                <p>Learn to build smart contracts and decentralized applications. Perfect for beginners and intermediate developers.</p>
            </div>
        </div>
    </section>

    <section id="resources">
        <h2><span class="red">Learning</span> Resources</h2>
        <div class="resources-grid">
            <div class="resource-card">
                <div class="resource-icon">📄</div>
                <h3>Bitcoin Whitepaper</h3>
                <p>Read Satoshi Nakamoto's original Bitcoin whitepaper - the document that started it all.</p>
                <a href="https://bitcoin.org/bitcoin.pdf" target="_blank" class="resource-button">Read Whitepaper</a>
            </div>
            <div class="resource-card">
                <div class="resource-icon">📖</div>
                <h3>Ethereum Docs</h3>
                <p>Comprehensive documentation for understanding Ethereum and smart contracts.</p>
                <a href="https://ethereum.org/en/developers/docs/" target="_blank" class="resource-button">Explore Docs</a>
            </div>
            <div class="resource-card">
                <div class="resource-icon">🎓</div>
                <h3>Crypto Education</h3>
                <p>Free courses and tutorials to learn blockchain development and crypto trading.</p>
                <a href="https://www.coinbase.com/learn" target="_blank" class="resource-button">Start Learning</a>
            </div>
            <div class="resource-card">
                <div class="resource-icon">📰</div>
                <h3>Industry News</h3>
                <p>Stay updated with the latest crypto news and market analysis.</p>
                <a href="https://cointelegraph.com/" target="_blank" class="resource-button">Read News</a>
            </div>
        </div>
    </section>

    <section id="team">
        <h2><span class="red">Our</span> Team</h2>
        <div class="team-grid">
            <div class="team-member">
                <div class="team-photo">NH</div>
                <h3>Nick Hardy</h3>
                <p class="role">President & Co-Founder</p>
            </div>
            <div class="team-member">
                <div class="team-photo">AB</div>
                <h3>Adu Batta</h3>
                <p class="role">Co-Founder & VP of Operations</p>
            </div>
            <div class="team-member">
                <div class="team-photo">TE</div>
                <h3>Tharun Ekabaram</h3>
                <p class="role">Co-Founder & VP of Strategy</p>
            </div>
            <div class="team-member">
                <div class="team-photo">MG</div>
                <h3>Michael Green</h3>
                <p class="role">Co-Founder & VP of Partnerships</p>
            </div>
            <div class="team-member">
                <div class="team-photo">JB</div>
                <h3>James Bang</h3>
                <p class="role">Co-Founder & VP of Partnerships</p>
            </div>
        </div>
    </section>

    <section id="contact">
        <h2><span class="red">Get In</span> Touch</h2>
        <div class="contact-info">
            <p>Have questions? Reach out to us!</p>
            <div class="contact-spacing">
                <p>Email: <a href="mailto:blockchainatiu@gmail.com">blockchainatiu@gmail.com</a></p>
            </div>
            <p><a href="https://groupme.com/join_group/111029466/mh2tLBjm" target="_blank" class="btn-secondary">Join Our GroupMe →</a></p>
        </div>
    </section>

    <footer>
        <p>&copy; 2025 Blockchain Club at IU. All rights reserved.</p>
        <div class="social-links">
            <a href="https://groupme.com/join_group/111029466/mh2tLBjm" target="_blank">GroupMe</a>
            <a href="mailto:blockchainatiu@gmail.com">Email Us</a>
        </div>
    </footer>

    <script>
        // Enhanced Animated Network Background
        const canvas = document.getElementById('network-canvas');
        const ctx = canvas.getContext('2d');
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;

        const particles = [];
        const particleCount = 120;
        const mouse = { x: null, y: null, radius: 150 };

        window.addEventListener('mousemove', (e) => {
            mouse.x = e.x;
            mouse.y = e.y;
        });

        class Particle {
            constructor() {
                this.x = Math.random() * canvas.width;
                this.y = Math.random() * canvas.height;
                this.vx = (Math.random() - 0.5) * 0.8;
                this.vy = (Math.random() - 0.5) * 0.8;
                this.radius = Math.random() * 2.5 + 1;
            }

            update() {
                this.x += this.vx;
                this.y += this.vy;

                if (this.x < 0 || this.x > canvas.width) this.vx *= -1;
                if (this.y < 0 || this.y > canvas.height) this.vy *= -1;

                // Mouse interaction
                const dx = mouse.x - this.x;
                const dy = mouse.y - this.y;
                const distance = Math.sqrt(dx * dx + dy * dy);
                
                if (distance < mouse.radius) {
                    const angle = Math.atan2(dy, dx);
                    const force = (mouse.radius - distance) / mouse.radius;
                    this.x -= Math.cos(angle) * force * 2;
                    this.y -= Math.sin(angle) * force * 2;
                }
            }

            draw() {
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.radius, 0, Math.PI * 2);
                ctx.fillStyle = 'rgba(153, 0, 0, 0.6)';
                ctx.fill();
            }
        }

        for (let i = 0; i < particleCount; i++) {
            particles.push(new Particle());
        }

        function animate() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);

            particles.forEach(particle => {
                particle.update();
                particle.draw();
            });

            particles.forEach((p1, i) => {
                particles.slice(i + 1).forEach(p2 => {
                    const dx = p1.x - p2.x;
                    const dy = p1.y - p2.y;
                    const distance = Math.sqrt(dx * dx + dy * dy);

                    if (distance < 120) {
                        ctx.beginPath();
                        ctx.strokeStyle = `rgba(153, 0, 0, ${0.3 - distance / 400})`;
                        ctx.lineWidth = 1;
                        ctx.moveTo(p1.x, p1.y);
                        ctx.lineTo(p2.x, p2.y);
                        ctx.stroke();
                    }
                });
            });

            requestAnimationFrame(animate);
        }

        animate();

        window.addEventListener('resize', () => {
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
        });

        // Fetch Live Crypto Prices with error handling
        async function fetchCryptoPrices() {
            try {
                const response = await fetch('https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum,cardano,solana,polkadot,dogecoin&vs_currencies=usd&include_24hr_change=true');
                
                if (!response.ok) {
                    throw new Error('API response was not ok');
                }
                
                const data = await response.json();
                
                const cryptoMap = {
                    bitcoin: { name: 'Bitcoin', symbol: 'BTC' },
                    ethereum: { name: 'Ethereum', symbol: 'ETH' },
                    cardano: { name: 'Cardano', symbol: 'ADA' },
                    solana: { name: 'Solana', symbol: 'SOL' },
                    polkadot: { name: 'Polkadot', symbol: 'DOT' },
                    dogecoin: { name: 'Dogecoin', symbol: 'DOGE' }
                };

                const container = document.getElementById('cryptoPrices');
                container.innerHTML = '';

                Object.keys(data).forEach(key => {
                    const crypto = data[key];
                    const info = cryptoMap[key];
                    
                    if (crypto && crypto.usd) {
                        const change = crypto.usd_24h_change || 0;
                        const changeClass = change >= 0 ? 'positive' : 'negative';
                        const changeSymbol = change >= 0 ? '▲' : '▼';

                        const cryptoItem = document.createElement('div');
                        cryptoItem.className = 'crypto-item';
                        cryptoItem.innerHTML = `
                            <div class="crypto-name">${info.name} (${info.symbol})</div>
                            <div class="crypto-price">${crypto.usd.toLocaleString(undefined, {minimumFractionDigits: 2, maximumFractionDigits: 2})}</div>
                            <div class="crypto-change ${changeClass}">
                                ${changeSymbol} ${Math.abs(change).toFixed(2)}%
                            </div>
                        `;
                        container.appendChild(cryptoItem);
                    }
                });
            } catch (error) {
                console.error('Error fetching crypto prices:', error);
                const container = document.getElementById('cryptoPrices');
                container.innerHTML = '<div class="loading">Unable to load live prices at the moment. Please try again later.</div>';
            }
        }

        // Initial fetch
        fetchCryptoPrices();
        
        // Update every 60 seconds
        setInterval(fetchCryptoPrices, 60000);

        // Time Clock - EST Timezone
        function updateTime() {
            const now = new Date();
            
            // Convert to EST (UTC-5) or EDT (UTC-4) based on daylight saving
            const estOffset = -5; // EST is UTC-5
            const edtOffset = -4; // EDT is UTC-4
            
            // Simple daylight saving detection (second Sunday in March to first Sunday in November)
            const year = now.getUTCFullYear();
            const march = new Date(year, 2, 8); // March 8th
            const november = new Date(year, 10, 1); // November 1st
            
            // Find second Sunday in March
            const marchSecondSunday = march.getDate() + (7 - march.getDay()) % 7 + 7;
            const marchDST = new Date(year, 2, marchSecondSunday);
            
            // Find first Sunday in November
            const novemberFirstSunday = november.getDate() + (7 - november.getDay()) % 7;
            const novemberDST = new Date(year, 10, novemberFirstSunday);
            
            const isDST = now >= marchDST && now < novemberDST;
            const offset = isDST ? edtOffset : estOffset;
            
            // Apply offset
            const estTime = new Date(now.getTime() + (offset * 60 * 60 * 1000));
            
            const timeString = estTime.toLocaleTimeString('en-US', {
                hour12: true,
                hour: '2-digit',
                minute: '2-digit',
                second: '2-digit'
            });
            const dateString = estTime.toLocaleDateString('en-US', {
                weekday: 'short',
                month: 'short',
                day: 'numeric'
            });
            
            document.getElementById('current-time').textContent = timeString;
            document.getElementById('current-date').textContent = dateString;
        }
        
        // Update time immediately and then every second
        updateTime();
        setInterval(updateTime, 1000);
        
        // Night Mode Toggle
        const nightModeToggle = document.getElementById('nightModeToggle');
        let isNightMode = false;
        
        // Load saved theme preference
        const savedTheme = localStorage.getItem('theme');
        if (savedTheme === 'light') {
            isNightMode = true;
            document.body.classList.add('light-mode');
            nightModeToggle.textContent = '☀️';
            nightModeToggle.classList.add('active');
        }
        
        nightModeToggle.addEventListener('click', toggleTheme);
        
        function toggleTheme() {
            isNightMode = !isNightMode;
            const body = document.body;
            
            if (isNightMode) {
                // Light mode
                body.classList.add('light-mode');
                nightModeToggle.textContent = '☀️';
                nightModeToggle.classList.add('active');
                localStorage.setItem('theme', 'light');
            } else {
                // Dark mode (default)
                body.classList.remove('light-mode');
                nightModeToggle.textContent = '🌙';
                nightModeToggle.classList.remove('active');
                localStorage.setItem('theme', 'dark');
            }
        }
        

        // Smooth Scrolling
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            });
        });

        // Intersection Observer for animations
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);

        document.querySelectorAll('.card, .event-item, .resource-card, .team-member').forEach(el => {
            el.style.opacity = '0';
            el.style.transform = 'translateY(20px)';
            el.style.transition = 'opacity 0.6s ease-out, transform 0.6s ease-out';
            observer.observe(el);
        });
    </script>
</body>
</html>
